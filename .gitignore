# dotcontext runtime state
.context/plans/
.context/cache/semantic/
.context/runtime/
.context/**/archive/

# deps e logs locais
node_modules/
*.log
dev-server.log

# artefatos de runs de documentos (gerados localmente)
output/
working/

# estado de workflow do impeccable (evidências locais de revisão)
.impeccable/

# build — nunca versionar bundle com segredos VITE_ embutidos (Google API Key, etc)
dist/
.agents/

# chaves locais — NUNCA commitar segredos (.env.* ignorado, só o exemplo versionado)
.env
.env.*
!.env.example

